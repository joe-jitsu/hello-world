let total = 0; 
let count = 0; 

while (count <= 10) {
  total = total + count; 
  count += 1; 
}
console.log(total)

// count =1  = every execution, count will have the value of 1.

// count +=1 = every execution, 1 will be added to the value of count.  